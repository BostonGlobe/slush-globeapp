$assets-path = '../assets/'

//=======================
// Base
//=======================
@require 'base/variables'
@require 'base/helpers'
@require 'base/colors'
@require 'base/layout'
@require 'base/fonts'

//=======================
// Template Fonts
//=======================
// See base/fonts for full listing
// Set other fonts in main.styl
// Note: all projects require Benton Bold and Miller-Regular so don't change em
set-font('benton-bold')
set-font('miller-regular')

//=======================
// Template Components
//=======================
@require 'template/*'

//=======================
// Custom styles
//=======================
@require 'graphic/*'

// Combine media queries (Don't remove)
apply_mq_cache()